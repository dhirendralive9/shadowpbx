<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' — ' : '' %>ShadowPBX</title>
  <link rel="stylesheet" href="/css/style.css">
  <% if (typeof extraHead !== 'undefined') { %><%- extraHead %><% } %>
</head>
<body>
  <% if (typeof noLayout === 'undefined' || !noLayout) { %>
  <div class="layout">
    <%- include('header') %>
    <%- include('tabs', { active: typeof active !== 'undefined' ? active : 'dashboard' }) %>
    <main>
</head>
<% } %>
